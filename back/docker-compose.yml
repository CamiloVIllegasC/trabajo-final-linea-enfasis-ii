services:

  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
  back:
    build: .
    restart: always
    ports:
      - "5000:5000"
    depends_on:
      - db

volumes:
  db_data:
  backend: