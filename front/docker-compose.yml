services:
  front:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000/api}
    image: citas-front:latest
    container_name: citas-front
    ports:
      - "5173:80"
    restart: unless-stopped

  